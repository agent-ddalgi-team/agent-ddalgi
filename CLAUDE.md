# 파일 관리 규칙

이 저장소에서 AI 도구(Claude Code 등)가 작업할 때 지키는 규칙이다.
팀원 4명이 비전공자이므로, **파일이 왜 생기고 왜 사라지는지 사람이 바로 알 수 있게** 하는 것이 목적이다.

## 1. 작업 시작 시 현재 상태부터 확인한다

- 현재 브랜치와 **이미 있던 미커밋 변경**을 먼저 확인한다.
- 작업을 시작하기 전부터 있던 변경과 이번 작업으로 생긴 변경을 **반드시 구분해서** 보고한다.
- 다른 사람이 만들어 둔 변경을 임의로 되돌리거나 덮어쓰지 않는다.

## 2. 새 파일을 만들기 전에 기존 것을 먼저 본다

- 같은 목적의 폴더·파일이 이미 있는지 확인하고, 중복해서 만들지 않는다.
- 새 파일이 꼭 필요하면 **만들기 전에** 다음 세 가지를 설명하고 확인을 받는다.
  1. 어느 경로에 만들 것인지
  2. 무엇에 쓰는 파일인지
  3. 기존 파일로는 왜 해결할 수 없는지

## 3. 일일 작업일지·당일 작업지시서는 저장소에 만들지 않는다

- 이 문서들은 **구글드라이브에서 관리한다.**
- 저장소 안에 자동으로 만들지 않는다. 요청받지 않은 작업일지·완료 확인표를 새로 만들거나,
  기존 기록 문서를 자동으로 갱신하지 않는다.

## 4. 일지 파일을 요청받았을 때

- 저장소 **밖의 지정된 경로**에 만든다.
- 경로가 정해지지 않았다면 파일을 만들지 말고 **대화창에 먼저 작성**한 뒤, 어디에 저장할지 확인받는다.

## 5. 일회성 내용은 대화창에 쓴다

- 설명, 점검 보고, 작업 요약, 비교표 같은 한 번 쓰고 마는 내용은 **대화창에 작성한다.**
- 별도 `.md` 파일로 자동 저장하지 않는다.

## 6. 오래 쓸 설명은 기존 문서를 고친다

- 계속 참고할 개발 설명이라면 새 문서를 만들기보다 **기존 문서 갱신을 우선**한다.
- 다만 지난 작업을 기록한 문서(`docs/`의 날짜별 기록 등)는 **그 시점의 사실을 남긴 것**이므로,
  요청받지 않은 한 뒤늦게 고치지 않는다. 새로 알게 된 내용은 현재 시점 문서에 적는다.

## 7. 옮기거나 지우기 전에 세 가지를 확인한다

1. **실행 의존성** — 프로그램이 실행 중에 실제로 읽는 파일인가
2. **문서 참조** — 다른 문서나 주석이 이 파일을 가리키는가
3. **유일한 정보** — 다른 곳에 사본이 있는지 **직접 확인한다.** 있을 것이라고 추정하지 않는다

### 참고: 실행에 필요한 파일 (2026-09-20 확인 기준)

아래는 이름만 보면 임시 파일·샘플처럼 보이지만 **없으면 서버가 시작조차 되지 않는다.**
정리 목적으로 옮기거나 지우지 않는다.

| 파일 | 읽는 코드 | 역할 |
|---|---|---|
| `fixtures/mock_profile.json` | `backend/agent.py`, `backend/profile_builder.py`, `backend/validators.py` | 본문 13개 항목의 순서·제목 원본 |
| `contracts/profile.schema.json` | 위 세 파일 | 결과 JSON 규격 |
| `prompts/extract.txt`, `prompts/draft.txt` | `backend/agent.py` | 실행용 프롬프트 |

이 표는 **확인 시점의 사실**이며 코드가 바뀌면 달라질 수 있다.
위 규칙(실행 의존성 확인)이 우선이고, 표는 참고용이다. 최신 상태는 이렇게 다시 확인한다.

```bash
grep -rn "ROOT / " backend/*.py        # 코드가 실행 중 여는 경로
```

## 8. 담당자 구분과 기존 변경을 존중한다

- 파일마다 주로 작업해 온 사람이 다르다. 누가 무엇을 만들었는지 확인하고,
  **관계없는 파일을 함께 고치지 않는다.**
- 작업상 다른 사람의 파일을 수정해야 하면 **이유와 범위를 먼저 설명**하고 확인받는다.
  설명하고 합의하면 누구든 수정할 수 있다. 아래 표는 **권한을 나누는 것이 아니라 누구와 상의할지를 알려 주는 것**이다.
- 규격 파일(`contracts/`)은 여러 코드가 동시에 의존하므로, 바꿀 때 팀 전체와 먼저 상의한다.

**현재 주 작성자 (2026-09-20 기준, 변동 가능)**

| 영역 | 주 작성자 |
|---|---|
| `backend/agent.py`, `prompts/`, 추출·본문 테스트 스크립트 | A |
| `backend/main.py`, `backend/profile_builder.py`, `backend/parsers.py`, `docs/`, `handoff/` | C |
| `backend/validators.py`, `backend/document_generator.py` | D |
| `contracts/` | 팀 공통 |

최신 상태는 `git log -1 --format='%an' -- <파일>`로 확인한다.

## 9. 작업이 끝나면 대화창에 보고한다

다음을 빠짐없이 적는다.

- 추가·수정·삭제한 파일 **전부** (`private_runs/` 등 **Git에서 제외된 파일도 포함**)
- 각 변경을 한 이유
- 검증 결과 — 실제로 실행한 것만. 하지 않은 것은 "미확인"으로 남긴다
- 남은 일

작업 시작 전부터 있던 변경은 이번 작업과 **구분해서** 적는다.
