# 2026-09-18(금) 완료 상태 구분 — A 담당

실행 근거는 `작업기록/today_0918.md`. 여기에는 **상태 판정만** 적는다.

> 판정 원칙: 코드 작성, 가짜 입력 + 실제 LLM 통합, 실제 기업 자료 검증, MD 성공,
> DOCX 성공, 화면 대조, 사람 검토, Git 공유는 **서로 다른 상태**다.
> 하나를 근거로 나머지를 완료로 적지 않는다. 전체 프로젝트 완료를 뜻하지 않는다.

> **공식 기록 위치**: 이 문서가 공식 판정 기록이다. `docs/acceptance_0918.md`는 안내문으로 정리했다.

## 0. 상태 구분 정의

| 구분 | 뜻 |
|---|---|
| **담당자 완료 보고** | 담당자가 자기 몫의 작업을 마쳤다고 보고한 상태 |
| **근거 확인** | A가 실제 파일·로그를 읽어 확인한 상태 |
| **근거 취합 대기** | 완료 보고는 있으나 A가 그 근거를 아직 연결하지 못한 상태 |
| **수행하지 않음(확인됨)** | 담당자 본인 기록에 "오늘 하지 않았다"고 적힌 상태 |

## 1. 이번 시험으로 판정한 것

| 항목 | 상태 | 근거 |
|---|---|---|
| **A 최소 테스트 2번** (입력 변경이 결과에 반영) | **완료** | 회사명·사업 분야 변경이 회사정보·본문·MD 모두에 반영됨. 기준 작업의 값은 variant 결과에 남아 있지 않음 |
| **이번 API·MD 시험** | **완료** | 자동 대조 검사 26건, MD 파일 검사 8건 통과. 저장 파일을 다시 열어 대조한 결과에 근거함 |

두 판정 모두 **가짜 테스트 자료 기준**이다.

## 2. 미확인 / 미완료로 남기는 것

| 항목 | 상태 | 이유 |
|---|---|---|
| **생성 문장 의미에 대한 사람 검토** | **미확인** | 자동 검사는 `fact_ids`가 supported 사실을 가리키는지와 인용문이 원문에 있는지만 본다. 의미·최신성·기업 승인은 보장하지 않는다. 비공개 비교표를 만들어 두었으나 **AI가 표를 만든 것은 사람 검토가 아니다** |
| **R08 전체** (끝까지 실행) | **완료 — C 검증 환경의 가짜 입력 2종 통합 시험 범위** | 아래 3-1 참고. C의 두 작업이 직접 근거, A의 시험은 보조 근거 |
| **R06** (MD/DOCX 파일) | **파일 생성·저장·열기 완료 / C 실행의 무재호출 직접 근거 미확인** | 아래 3-2 참고 |
| **DOCX** | **작업별로 구분** | C의 두 작업은 확인됨 / **A의 `0d40cd02-…`는 미확인 유지**. 아래 3-2 참고 |
| **R10** (자료 허용·검토) | **미확인 유지** | 기업 원문의 외부 LLM 입력 허용 근거를 받지 못했다 |
| **실제 기업 자료 검증** | **미완료 유지** | R10이 미확인이므로 착수하지 않았다. 이번 시험으로 완료 처리하지 않는다 |
| **R09** (코드 공유·재현) | **이번 시험만으로 완료 처리하지 않음** | 코드는 PR을 거쳐 반영됐으나, 두 저장소의 실행법·재현 절차 확인은 이번 범위 밖이다 |

## 3-1. R08 — 완료 범위와 제한

**최종 판정: 완료.** 범위는 **"C 검증 환경의 가짜 입력 2종 통합 시험"** 이다.

| 근거 구분 | 작업 | 상태 |
|---|---|---|
| **직접 근거** | C 기준 `bc4d7336-…` / C variant `bb47e300-…` | API 결과·MD·DOCX 3자 대조 일치, 입력별로 다른 결과 → **완료** |
| 위 두 job의 화면 내용 대조 | — | C 기록에 job 단위 연결 없음 → **미확인** |
| **보조 근거** | A 기준 `1009d15b-…` / A variant `0d40cd02-…` | API·MD 대조(자동 26건 + MD 8건) → 보존 |
| B의 브라우저 직접 확인 | — | **미수행 · 월요일 이월** (B 기록 그대로 유지) |

제한 사항:
- 가짜 테스트 자료 기준이며 **C 검증 환경 기준**이다.
- A와 C의 작업을 하나의 증거로 합치지 않았다. `fact_id`도 작업 사이에서 섞지 않았다.
- 브라우저 다운로드 성공을 화면 내용 대조로 확대하지 않았다.
- **항목 배정 이슈(4-1 이슈 1)가 미해결이다.** R08 완료가 전체 품질이나
  실제 기업 자료 검증의 완료를 뜻하지 않는다.

## 3-2. R06·DOCX — 근거 수준 구분

| 구성 요소 | 상태 | 근거 수준 |
|---|---|---|
| MD/DOCX 생성·저장·열기 | **완료** | C 기록: 브라우저 저장 다운로드 성공, 워드 열람·한글 정상, 3자 대조 일치 |
| C 실행의 문서 생성 중 LLM 추가 호출 0회 | **직접 근거 미확인** | C 근거는 `run_meta.json` note·`written_at` 불변 + 코드 확인. **코드상 무재호출**과 **간접 정황**이며 실제 호출 수 집계가 아니다 |
| A 실행의 문서 생성 중 LLM 추가 호출 0회 | **A 증거로 유지** | A 환경 서버 로그의 누적 호출 수 불변 |

| DOCX 대상 | 상태 |
|---|---|
| C 기준 `bc4d7336-…` | **확인됨** (37,494 B) |
| C variant `bb47e300-…` | **확인됨** (37,505 B) |
| A 기준 `1009d15b-…` | 생성 성공 기록 있음 (A 환경) |
| **A variant `0d40cd02-…`** | **미확인 유지** — C의 확인이 이 job을 대체하지 않는다 |

근거를 보충하려고 새 시험이나 호출을 하지 않았다.

## 3. 품질 검토 항목 (결함 아님, 수정하지 않음)

- **사업 분야 본문의 문장 형태 차이**: variant 작업의 사업 분야 섹션 본문이 서술문 없이
  값만으로 생성됐다. 기준 작업은 서술문 형태였다.
  근거·`fact_ids` 규칙 위반은 아니며 자동 검사도 통과했다.
  **이번에 프롬프트를 고치거나 본문을 재생성하지 않았다.** 사람 검토 시 함께 볼 항목으로 남긴다.

## 4. 담당자별 상태 (2026-09-18 취합 기준)

B는 9/18 작업 완료를 보고했고 기록도 확인했다. 아래 "수행하지 않음"은 **B 본인이 기록한 내용**이며,
A가 근거 부족을 이유로 미완료로 단정한 것이 아니다.

| 담당 | 항목 | 상태 | 근거 |
|---|---|---|---|
| B | 9/18 프론트 작업 | **담당자 완료 보고 + 근거 확인** | `agent-ddalgi-frontend` `origin/develop`의 9/18 기록 3건 |
| B | 자동 대조 13/13, 코드 검토 12건, 빌드·lint·format 검사, 기록 병합 | **완료** | 같은 기록 |
| B | R05 브라우저 직접 확인 | **미수행 · 월요일 이월** | "오늘 브라우저 확인을 수행하지 않았고 월요일로 이월했다" |
| B | R06 문서 다운로드 | **시도했으나 성공하지 못함** | B 환경에서 MD/DOCX 모두 `409 DOCUMENT_FAILED`. "시도하지 않음"과 구분한다 |
| B | R08 입력 2종 화면 대조 | **미수행 · 월요일 이월** | B는 mock 모드라 "미확인, 판정 아님"으로 기록 |
| B | 커밋·푸시·PR | **시점 구분** | B 기록 작성 시점 "미푸시" → 현재 시점 `origin/develop`에 **병합됨**(PR #2) |
| C | 9/18 백엔드·문서 출력 | **완료 보고 + 근거 확인** (취합 대기 해소) | C가 전달한 일일 작업일지 `260918_이강욱_백엔드_문서출력.md`를 A가 직접 읽음. 확인 범위: 기준 `bc4d7336-…`·variant `bb47e300-…`의 API·MD·DOCX 대조 |
| C | 기록의 저장소·브랜치·커밋 | **미확인** | C 기록에 없음. 추정하지 않음 |
| C | 두 job의 화면 내용 대조 | **미확인** | 브라우저 확인 기록은 있으나 job 단위 연결 없음 |
| A | variant API·MD 시험 | **근거 확인** | `작업기록/today_0918.md` |

B에게 같은 완료 보고를 다시 요청하지 않는다.
C에게는 추가 시험·재개발이 아니라 **기존 기록의 위치만** 요청한다.

## 4-1. A 후속 이슈 (미해결, 이번에 수정하지 않음)

**이슈 1 — `processes` / `process_count` 항목 배정 불일치**

| 작업 | 환경 | 공정 목록(`processes`) | 공정 수(`process_count`) | 출처 |
|---|---|---|---|---|
| A 기준 `1009d15b-…` | A | `not_found` | `conflict` | A `result.json` |
| A variant `0d40cd02-…` | A | `not_found` | `conflict` | A `result.json` |
| C 기준 `bc4d7336-…` | C | 추가 확인 필요 (C 주석: conflict) | 추가 확인 필요 (C 주석: conflict) | C 작업일지 |
| C variant `bb47e300-…` | C | 추가 확인 필요 (C 주석: conflict) | 자료에서 확인되지 않음 (not_found) | C 작업일지 |

- C 값은 **문서 본문의 안내 문구**다. "추가 확인 필요"는 `conflict`와 `needs_confirmation`
  양쪽에 쓰이므로 문서만으로 둘을 구분할 수 없다. 괄호 안은 C의 주석이며 A가 JSON으로 재확인한 값이 아니다.
- **형식 검사 통과와 항목 의미 정확성은 다르다.** 네 작업 모두 개수 규칙을 지켰고 `INVALID_OUTPUT`도 없었다.
- A와 C의 **입력·코드·프롬프트·모델·설정 동일성이 확인되지 않았다.**
  원인을 "동일 조건의 반복 실행 불안정성"으로 단정하지 않는다.
- **추출 의미 정확성 관련 미해결 이슈로 남긴다.**

**이슈 2 — 본문의 라벨 접두어 잔존**

C 기록이 지적한 "회사 개요:", "사업 분야:" 접두어. **출력 형식 검토 항목으로 기록한다.**
A가 앞서 관찰한 문장 표현 차이와 같은 원인이라고 단정하지 않는다.

상세 증거는 비공개 경로(`private_runs/<job_id>/`)에만 있다.

### 환경 차이 기록

B의 확인은 **로컬 사본 백엔드의 mock 모드** 기준이다. 현재 팀 `origin/develop`에는
D의 `document_generator.py`와 A의 `draft_profile`이 있어 문서 API가 동작한다.
B가 본 409는 B 환경 기준 사실이며, 재시험을 요청하지 않는다.

## 5. A가 실제로 해야 할 일

- 비공개 비교표(`private_runs/0d40cd02-…/human_review_checklist.md`)로 핵심 문장의 의미를 직접 검토.
  **AI가 표를 만든 것은 사람 검토가 아니다.** 검토자·일시·판정 칸이 비어 있으면 미확인이다.
- 기업 자료 사용 허용(R10) 근거를 자료 담당자에게 확인. 확인 전까지 R10과
  실제 기업 자료 검증은 현재 상태를 유지한다.
- C의 9/18 기록 위치를 받아 이 표에 연결.
