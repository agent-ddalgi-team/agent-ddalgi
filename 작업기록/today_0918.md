# 2026-09-18(금) 실행 기록 — A 담당 (두 번째 가짜 입력 API·MD 시험)

이 문서는 **두 번째 가짜 입력(variant)의 통합 시험 결과**를 정리한 기록이다.
입력은 전부 가짜 테스트 자료이며 실제 기업 자료는 사용하지 않았다.
원문과 생성 본문은 이 문서에 옮기지 않는다 — 실제 문자열은 아래 비공개 비교표에서 본다.

> **공식 기록 위치**: 이 문서와 `작업기록/acceptance_0918.md`가 공식 기록이다.
> `docs/today_0918.md`·`docs/acceptance_0918.md`는 이 문서를 가리키는 안내문으로 정리했다.
> 단 이미 푸시된 커밋 `ac01c0a`에는 이전 내용이 그대로 남아 있다(9절 참고).

## 0. 오늘 반영한 코드 작업 (요약)

| 커밋 | 내용 | PR | 상태 |
|---|---|---|---|
| `a911b88` | fix: 추출 프롬프트의 상태별 facts 규칙 보강 | #7 | `origin/develop` 반영 |
| `342c2a8` | feat: 회사소개서 본문 생성 및 검증 기능 추가 | #8 | `origin/develop` 반영 |

- 프롬프트 보강: `needs_confirmation`인데 `facts`가 빈 배열로 와서
  `INVALID_OUTPUT[status_fact_count]`가 간헐 발생했다. 개수 규칙이 모델에 전달되지 않는 것이
  원인이어서 `prompts/extract.txt`에만 상태별 개수 하한과 단서를 추가했다(코드 변경 없음).
- 본문 생성: `backend/agent.py`에 `draft_profile(supported_facts) -> draft_sections` 추가.
  supported 섹션만 생성하고, `company_name`은 단독 섹션이 없어 대상에서 제외한다.
  누락·상충 항목의 고정 안내 문구는 C(`profile_builder`)가 붙이므로 본문이 같은 문구를 만들면 거부한다.
- 검증: D의 `backend/validators.py`를 **재사용**했고 새로 만들지 않았다.

## 1. 대상 작업

두 작업 모두 **가짜 입력을 사용한 llm 실행**이다. 고정 Mock 실행이 아니다.

| 구분 | 전체 job_id | 입력 파일 | 실행 모드 |
|---|---|---|---|
| 기준 | `1009d15b-d00d-448c-9398-f8b1b39c6c07` | `mock_source_a.txt`, `mock_source_b.txt` | `agent_mode=llm`, `llm_called=true`, `input_kind=fake_test_data` |
| variant | `0d40cd02-dbaa-4913-ae92-be3759539f5f` | `day2_variant_source_a.txt`, `day2_variant_source_b.txt` | `agent_mode=llm`, `llm_called=true`, `input_kind=fake_test_data` |

실행 모드 값은 각 작업의 `run_meta.json`에서 확인했다.

## 2. variant 실행 조건

- `POST /api/profiles` **1회만** 실행. 실패 시 재업로드하지 않는 조건으로 진행했다.
- `company_name_hint` **미사용**(보내지 않음). 기준 작업은 `"테스트 회사"`를 보냈다.
- 두 파일을 하나의 요청에 함께 업로드했다.
- 같은 job_id로만 조회했고 `ready`에서 멈췄다.
- 서버: `AGENT_MODE=llm`, 프로젝트 `.venv`, `backend.main:app`, `127.0.0.1:8000`, `--reload` 없음.

## 3. 검사 결과

| 검사 | 결과 | 근거 |
|---|---|---|
| 자동 대조 검사 | **26건 통과 / 실패 0** | 저장된 `result.json`·`agent_input.json`·`run_meta.json`을 다시 읽어 재확인 |
| MD 파일 검사 | **8건 통과 / 실패 0** | 저장된 MD 파일을 다시 열어 재확인 |

자동 검사에 포함된 항목: 상태 `ready`·`error` null, `is_mock=true`,
`human_review_required=true`, 고정 Mock 미대체, 회사명·사업 분야의 변경 반영,
기준 작업과의 차이, 상태 분포(supported 3 / conflict 1 / needs_confirmation 1 / not_found 9),
`process_count`의 상충 유지, 근거 위치·인용문, `fact_id` 중복·재부여 없음,
본문 13개 섹션·순서, 본문 `fact_ids`가 supported만 참조, 고정 안내문의 `fact_ids`가 빈 목록,
공통 스키마, `validate_draft`, `validate_profile_result`.

MD 검사에 포함된 항목: 파일 열기, 13개 섹션 제목 존재, 섹션 순서가 최종 JSON과 일치,
13개 문단 text가 그대로 포함, 회사명·사업 분야 변경 반영, 이전 값 부재, 테스트 데이터 표시 유지.

> **이전 보고와의 차이**: 앞선 실행 보고에는 자동 검사가 "30건"으로 적혀 있었으나,
> 같은 검사를 다시 돌려 세어 보니 **26건**이다. 30은 보고 작성 시의 계수 오류다.
> 검사 내용과 통과 여부는 달라지지 않았다.

## 4. 실제 외부 LLM 호출과 토큰

서버 로그(`backend.agent`의 `openai call`, httpx의 `api.openai.com` 요청)로 확인했다.

| # | 단계 | status | input | output | total |
|---|---|---|---|---|---|
| 1 | 추출 | completed | 1,272 | 472 | **1,744** |
| 2 | 본문 | completed | 690 | 82 | **772** |

- **실제 외부 LLM 요청 2건**, 둘 다 `200 OK`
- **내부 재시도 0회** (retry 로그 없음, 비200 응답 없음)
- **합계 2,516 토큰**
- **MD 생성으로 인한 추가 LLM 호출 0회** — 문서 생성 전후로 누적 호출 수가 변하지 않았다

### 호출 제한 관련 경위

사전 상한 보장 불가 안내 후 그대로 진행 결정. 이번 실제 요청은 2건이며 재시도는 0회.

안내한 내용은 `backend/agent.py`의 `OPENAI_MAX_RETRIES = 1` 때문에 논리 호출 1건당
최대 2번 요청이 가능하므로, "내부 재시도를 포함한 최대 2회"를 코드로 보장할 수 없다는 것이었다.
**이 결과를 "앞으로도 최대 2회가 보장됨"으로 해석하면 안 된다.** 이번 실행에서 재시도가
발생하지 않았을 뿐이며 설정은 그대로다.

## 5. 입력 변경의 반영

variant 작업의 **회사명**과 **사업 분야** 사실이 기준 작업과 다른 값으로 기록됐고,
같은 값이 본문 섹션과 MD 파일에도 반영됐다. 기준 작업의 사업 분야 값은 variant 결과에
남아 있지 않다. 실제 문자열은 이 문서에 옮기지 않으며 비공개 비교표에서 확인한다.

두 작업 모두 상태 분포와 `process_count`의 상충 처리는 같았고,
`fact_id`는 재부여 없이 유지됐다.

## 6. 파일 경로

```
최종 JSON : private_runs/0d40cd02-dbaa-4913-ae92-be3759539f5f/result.json
MD 파일   : private_runs/0d40cd02-dbaa-4913-ae92-be3759539f5f/documents/회사소개서_초안.md
비교표    : private_runs/0d40cd02-dbaa-4913-ae92-be3759539f5f/human_review_checklist.md
기준 작업 : private_runs/1009d15b-d00d-448c-9398-f8b1b39c6c07/
```

MD는 `HTTP 200`, `content-type: text/markdown; charset=utf-8`로 받았다.
저장 전에 JSON 오류 응답인지 먼저 검사해 정상 문서임을 확인한 뒤 저장했고, 파일을 열어 내용을 확인했다.
`private_runs/`는 `.gitignore`로 Git에서 제외된다.

## 7. 근거 구분

| 항목 | 저장된 파일·로그로 확인 | 이전 실행 보고에만 있음 |
|---|---|---|
| job_id, 입력 파일명, 실행 모드 | ✅ `run_meta.json`, `result.json`의 `sources` | |
| 자동 검사 26건, MD 검사 8건 | ✅ 저장 파일로 재실행해 확인 | |
| 호출 2건·재시도 0회·토큰 2,516 | ✅ 서버 로그 | |
| MD 추가 호출 0회 | ✅ 서버 로그의 누적 호출 수 | |
| 본문 문단 ↔ 원문 인용 연결 | ✅ 저장 원본 파일의 행 번호까지 재확인 | |
| `POST /api/profiles` 1회, hint 미사용 | | ⚠️ 실행 시점 기록. 서버 접근 로그에 요청 1건은 남아 있으나 요청 본문은 보존되지 않음 |
| 상태 변화(`ready`까지 약 1초) | | ⚠️ 조회 시점 기록. 최종 상태는 `result.json` 존재로 확인됨 |

근거가 없는 항목은 위와 같이 표시했고, 기록을 보충하려고 유료 재실행을 하지 않았다.

## 8. 변경 사항

- **코드·프롬프트·스키마·API 규격·설정 변경 없음**
- **서버 재시작·종료 없음**
- **서비스 외부 LLM 추가 호출 0회**
- 새로 만든 파일: 이 문서, `작업기록/acceptance_0918.md`, 위 비공개 비교표

### 단계별 Git 작업 구분

| 단계 | Git 작업 |
|---|---|
| 근거 취합·문서 정리 단계 | **커밋·푸시·이력 재작성 없음. `fetch`로 로컬 원격 추적 정보 갱신.** |
| 이번 공유 단계 | 문서 4개 커밋 1건 + `docs/a-record-0918`에 일반 푸시 + PR 생성 |

두 단계의 사실을 섞지 않는다.

## 9. B/C의 기존 완료 근거 취합

A가 **읽을 수 있는 저장소**는 백엔드 `agent-ddalgi`와 프론트 `agent-ddalgi-frontend` 두 곳이다.
다른 사람 PC나 제공되지 않은 저장소는 읽을 수 없으며, 읽을 수 있다고 가정하지 않았다.

| 담당 | 완료 보고 범위 | 근거 파일·위치 | 실제 job_id | 실행 모드 | 확인된 내용 | 아직 연결되지 않은 근거 |
|---|---|---|---|---|---|---|
| **B** | 9/18 프론트 작업 완료 보고 있음 | `agent-ddalgi-frontend` `origin/develop`의 `docs/B_프론트엔드_검증기록_0918.md`, `_작업기록_0918.md`, `_일일요약_0918.md` (커밋 `4a795cc`·`a08a588`, PR #2로 병합) | 기록에 job_id 명시 없음 | **mock** (본인 로컬 사본 백엔드) | 자동 대조·코드 검토·빌드 검사 **완료**, 기록 병합 **완료** | A의 두 job(`1009d15b`, `0d40cd02`)과 연결되는 화면 증거 없음 |
| **C** | 완료 보고 있음 | **접근 가능한 저장소에서 9/18자 C 기록을 찾지 못함** | — | — | — | 기존 기록의 저장소·브랜치·파일 경로 미확보 → **기존 완료 근거 취합 대기** |
| **A** | variant API·MD 시험 | 이 문서 | `1009d15b-…`, `0d40cd02-…` | **llm** (가짜 입력) | 위 1~7절 | — |

### B 기록에서 그대로 옮기는 내용 (B 본인의 판정)

B의 기록은 다음을 **B 스스로** 명시한다. A가 B를 미완료로 단정한 것이 아니며,
B에게 같은 완료 보고를 다시 요청하지 않는다.

- **완료한 것**: Mock 렌더링 자동 대조 13/13, 폴링·중복 클릭·다운로드 격리 코드 검토 12건,
  `tsc -b`·lint·build·format 검사 통과, 기록 문서 병합.
- **R05(브라우저 직접 확인)**: **미수행 · 월요일 이월**.
  "오늘 브라우저 확인을 수행하지 않았고 월요일로 이월했다"고 적혀 있다.
- **R06(문서 다운로드)**: B 환경에서 MD/DOCX 모두 `409 DOCUMENT_FAILED`.
  이는 **시도했으나 성공하지 못함**이다. "시도하지 않음"과 다르다.
- **R08(입력 2종 대조)**: mock 모드라 두 입력이 같은 결과 → "미확인, 판정 아님"으로 기록.
- **시점 구분**: B 기록 작성 시점에는 커밋·푸시·PR이 "미푸시"였고,
  **현재 시점에는 `origin/develop`에 병합되어 있다**(PR #2). 서로 다른 시점의 사실이다.

### 환경 차이 — 재시험 요청이 아니라 기록

B가 쓴 백엔드는 **로컬 사본**(`FINAL_geosan`)이고 모드는 `mock`이었다.
현재 팀 저장소 `origin/develop`에는 D의 `backend/document_generator.py`와 A의 `draft_profile`이
들어 있어 문서 API가 동작한다(A의 두 작업에서 MD 200 확인, 기준 작업은 DOCX도 200).
따라서 **B가 본 409는 B의 환경 기준 사실**이며, 현재 develop 기준 상태와는 다르다.
이 차이는 기록만 하고, 같은 시험을 다시 하라고 요청하지 않는다.

### R08 판정 근거 정리

R08은 "서로 다른 가짜 입력 2종에서 각 작업의 **API·화면·파일**이 연결되어 대조"를 요구한다.
**최종 판정과 근거 취합 상태는 다른 것이다.**

| 구성 요소 | 상태 |
|---|---|
| API 대조 (입력 2종) | **완료** — A가 두 job에서 확보 |
| 파일(MD) 대조 (입력 2종) | **완료** — A가 두 job에서 확보 |
| 브라우저 직접 확인 | **미수행 · 월요일 이월** — B 기록 기준 |
| C의 추가 근거 | **취합 대기** — 기록 위치 미확보 |
| **R08 최종 판정** | **미완료** (현재 확보한 근거 기준) |

"근거 취합 대기"는 남은 시험이 없다는 뜻이 아니다. 브라우저 직접 확인은 아직 수행되지 않았다.
C의 기록이 추가되면 그 증거에 따라 이 표를 갱신할 수 있다.
서로 다른 job_id의 JSON·화면·파일을 하나의 작업 증거처럼 섞지 않았다.

### DOCX (R06과 구분)

| 대상 | 상태 | 근거 |
|---|---|---|
| 기준 작업 `1009d15b` DOCX | 생성 성공 기록 있음 | 저장 파일 존재, 응답 200 |
| variant 작업 `0d40cd02` DOCX | **미확인** | 이번에 실행하지 않음 |
| R06 전체(화면에서 저장·열기) | **미완료** | B 기록 기준. 화면 경로 확인은 이월 |

## 10. 남은 결정 — 이미 푸시된 이력

`docs/today_0918.md`·`docs/acceptance_0918.md`의 **현재 파일**은 안내문으로 정리했다.
그러나 커밋 `ac01c0a`(브랜치 `docs/a-record-0918`, 원격 반영됨)에는 이전 내용이 그대로 남아 있다.
**현재 작업폴더 정리와 과거 이력 정리는 별개 상태다.** 이력 처리는 별도 승인이 필요하다.
