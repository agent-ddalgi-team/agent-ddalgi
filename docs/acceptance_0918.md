# 2026-09-18(금) 완료 판정 — A 담당분

실행 근거는 `docs/today_0918.md`. 여기에는 **판정만** 적는다.

> 판정 원칙: 코드 작성, 가짜 입력 + 실제 LLM 통합, 기업 자료 검증, MD 성공, DOCX 성공,
> 화면 대조, Git 공유는 **서로 다른 상태**다. 하나를 근거로 나머지를 완료로 적지 않는다.

## 요구사항 판정

| 번호 | 요구사항 | 판정 | 근거 / 남은 것 |
|---|---|---|---|
| R03 | 14개 정보·근거 | **완료 (가짜 자료 기준)** | 실제 LLM 추출 2회, 14개 키·4개 상태·원문 위치·인용문·고유 `fact_id` 확인. 실제 기업 자료는 미사용 |
| R04 | 소개서 본문 | **완료 (가짜 자료 기준)** | `draft_profile` 구현·병합(PR #8). supported 사실만 사용, 두 입력 모두 `ready` 도달 |
| R07 | 최종 결과 검사 | **완료 (재사용)** | D의 `validate_draft`·`validate_profile_result` 재사용. 두 실행 모두 오류 0건, 공통 스키마 통과 |
| R08 | 끝까지 실행 | **부분** | API·MD 대조는 두 입력 모두 확보. **React 화면 대조 증거 없음** → 전체 완료 아님 |
| R10 | 자료 허용·검토 | **미확인** | 기업 원문 외부 LLM 입력 허용 근거를 받지 못함. 가짜 자료만 사용 중 |

## A 최소 테스트 (업무서 6절)

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 정상 — 출처 있는 회사명·사업 분야가 본문에 반영, 사실 번호 유지 | **통과** | 두 실행 모두 F001~F003이 본문 `fact_ids`에 그대로 유지 |
| 2 | 변경 — 입력을 바꾸면 그 사실이 바뀐다 | **통과** | 회사명 `테스트 회사` → `두번째 테스트 회사`, 사업 분야 `테스트 사업 A` → `테스트 사업 B`가 정보·본문·MD 모두에 반영. 문장은 글자 단위로 같지 않아도 됨 |
| 3 | 거부 — 없는 `fact_id`, unsupported 사실, 원문에 없는 quote | **통과** | `check_draft_rules.py` 17건 거부 확인 (유료 호출 없음) |
| 4 | 빈 근거 — 쓸 사실이 없으면 LLM으로 꾸미지 않고 C가 고정 안내문 조립 | **통과** | 두 실행 모두 11개 섹션이 고정 안내문 + `fact_ids: []`. `supported_facts`가 없으면 `draft_profile`이 빈 목록 반환 |
| 5 | 의미 대조 — 사람이 핵심 문장을 비교 | **미완료** | 자동 검사는 `fact_ids` 참조와 인용문 존재만 확인. 의미·최신성·기업 승인은 보장하지 않음 |

## 상태별 완료 구분

| 상태 | 판정 |
|---|---|
| 코드 작성 | **완료** — PR #7, #8 develop 병합 |
| 고정 Mock 통합 | **완료** (이전 기록) |
| 가짜 입력 + 실제 LLM 통합 | **완료** — 서로 다른 가짜 입력 2종에서 `ready` 도달 |
| 실제 기업 자료 검증 | **미완료** — R10 미확인이므로 착수하지 않음 |
| MD 생성 | **완료** — 두 job 모두 200, 파일 열어 내용 확인 |
| DOCX 생성 | **1건만 확인** — 기준 job에서 성공. variant job은 이번에 확인하지 않음 |
| React 화면 대조 | **미확인** — B 확인 필요 |
| Git 공유 | **완료** — 코드는 전부 PR을 거쳐 develop 반영 |

## 이번에 확인하지 않은 것

- 생성 문장의 **의미**에 대한 사람 검토 (최소 테스트 5번)
- 본문 생성의 반복 안정성. 각 입력에서 1회씩만 실행했다
- supported 항목이 많거나 문단이 여러 개인 긴 입력
- variant job의 DOCX 생성
- 실제 기업 자료 전체

## 이어받을 항목

- **B**: variant job `0d40cd02-dbaa-4913-ae92-be3759539f5f` 화면 조회 → R08 화면 대조분
- **C**: 같은 job_id의 DOCX 생성 확인
- **A**: 최소 테스트 5번(의미 대조), R10 허용 여부 확인
